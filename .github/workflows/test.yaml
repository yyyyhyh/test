name: autoAction
on: [push]
jobs:
  Test-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: curl
        continue-on-error: true
        run: |
          echo -e '\n-------------------------------------------------\n'
          echo -e '\nhttp://103.30.17.32:8088/Content/img/UserDraw/drawPW1.aspx\n'
          curl -i -s -k --compressed -m 10 -X $'GET' -H $'Accept-Encoding: gzip, deflate' -H $'Connection: close' -d $'' $'http://103.30.17.32:8088/Content/img/UserDraw/drawPW1.aspx'
          echo -e '\n-------------------------------------------------\n'
