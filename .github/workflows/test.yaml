name: autoAction
on: [push]
jobs:
  Test-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: curl
        continue-on-error: true
        run: |
          echo -e '\n-------------------------------------------------\n'
          echo -e '\nhttps://101.132.174.96:8089/Content/img/UserDraw/drawPW1.txt\n'
          curl -i -s -k --compressed -m 10 -X $'GET' -H $'Accept-Encoding: gzip, deflate' -H $'Connection: close' -d $'' $'https://101.132.174.96:8089/Content/img/UserDraw/drawPW1.txt'
          echo -e '\n-------------------------------------------------\n'
